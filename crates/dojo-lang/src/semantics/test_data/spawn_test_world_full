//! > Test ok empty (expected to fail due to missing dojo_manifests_dir)

//! > test_runner_name
test_semantics

//! > expression
spawn_test_world_full!()

//! > expected
Missing(
    ExprMissing {
        ty: <missing>,
    },
)

//! > semantic_diagnostics
error: Plugin diagnostic: Failed to load models and namespaces, ensure you have run `sozo build` first.
 --> lib.cairo:2:1
spawn_test_world_full!()
^**********************^

//! > ==========================================================================

//! > Test bad params

//! > test_runner_name
test_semantics

//! > expression
spawn_test_world_full!("ns1")

//! > expected
Missing(
    ExprMissing {
        ty: <missing>,
    },
)

//! > semantic_diagnostics
error: Plugin diagnostic: Invalid arguments. Expected "spawn_test_world_full!()" with no arguments.
 --> lib.cairo:2:1
spawn_test_world_full!("ns1")
^***************************^
