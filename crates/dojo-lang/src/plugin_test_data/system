//! > Test expansion of the component contract.

//! > test_runner_name
ExpandContractTestRunner

//! > cairo_code
use array::ArrayTrait;

#[derive(Component)]
struct Position {
    x: felt252,
    y: felt252,
}

#[derive(Component)]
struct Player {
    name: felt252, 
}

#[system]
mod SpawnSystem {
    use traits::Into;   
    use starknet::contract_address::ContractAddressIntoFelt252;

    use dojo::commands::Spawn;
    use super::Position;
    use super::IPositionDispatcher;
    use super::IPositionDispatcherTrait;
    use super::Player;
    use super::IPlayerDispatcher;
    use super::IPlayerDispatcherTrait;

    fn execute(name: felt252) {
        let entity_id = Spawn::entity((420));
        let player = Spawn::bundle(entity_id, (
            Player { name: name },
            Position { x: 0, y: 0 },
        ));

        let literal = Spawn::bundle(420, (
            Player { name: name },
            Position { x: 0, y: 0 },
        ));

        let player_id = starknet::get_caller_address();
        let player = Spawn::bundle((420, player_id.into()), (
            Player { name: name },
            Position { x: 0, y: 0 },
        ));

        let invalid_args = Spawn::bundle((
            Player { name: name },
            Position { x: 0, y: 0 },
        ));

        let invalid_path = Spawn::bundle((0, 0, 0, 0, 0), (
            Player { name: name },
            Position { x: 0, y: 0 },
        ));
        return ();
    }
}

#[system]
mod MoveSystem {
    use array::ArrayTrait;
    use dojo::query::Query;
    use super::Position;
    use super::IPositionDispatcher;
    use super::IPositionDispatcherTrait;
    use super::Player;
    use super::IPlayerDispatcher;
    use super::IPlayerDispatcherTrait;

    fn move_inner(positions: @Array<usize>, index: u32, count: u32) {
        return move_inner(positions, index + 1_u32, count - 1_u32);
    }

    fn execute(player_id: felt252) {
        let positions_query = Query::<(Position, Player)>::ids();
        let players_query = Query::<Player>::ids();
        let player = Query::<Player>::entity((player_id));

        let mut bar = 123;
        let mut foo = ArrayTrait::<felt252>::new();
        foo.append(bar);

        move_inner(@positions_query, 0_u32, positions_query.len());

        if bar == 123 {
            return ();
        } else if bar == 0 {
            return ();
        } else {
            return ();
        }
    }
}

//! > generated_cairo_code
use array::ArrayTrait;

#[derive(Copy, Drop)]
struct Position {
    x: felt252,
    y: felt252,
}

#[abi]
trait IPosition {
    fn set(entity_id: (felt252, felt252, felt252, felt252), value: Position);
    fn get(entity_id: (felt252, felt252, felt252, felt252)) -> Position;
}

#[contract]
mod PositionComponent {
    use option::OptionTrait;
    use starknet::SyscallResult;
    use traits::Into;
    use traits::TryInto;
    use super::Position;

    impl PositionSerde of serde::Serde::<Position> {
        fn serialize(ref serialized: Array::<felt252>, input: Position) {
            serde::Serde::<felt252>::serialize(ref serialized, input.x);
            serde::Serde::<felt252>::serialize(ref serialized, input.y);
        }
        fn deserialize(ref serialized: Span::<felt252>) -> Option::<Position> {
            Option::Some(
                Position {
                    x: serde::Serde::<felt252>::deserialize(ref serialized)?,
                    y: serde::Serde::<felt252>::deserialize(ref serialized)?,
                }
            )
        }
    }

    impl StorageAccessPosition of starknet::StorageAccess::<Position> {
        fn read(
            address_domain: u32, base: starknet::StorageBaseAddress
        ) -> starknet::SyscallResult::<Position> {
            Result::Ok(
                Position {
                    x: starknet::storage_read_syscall(
                        address_domain, starknet::storage_address_from_base_and_offset(base, 0_u8)
                    )?,
                    y: starknet::storage_read_syscall(
                        address_domain, starknet::storage_address_from_base_and_offset(base, 1_u8)
                    )?,
                }
            )
        }
        fn write(
            address_domain: u32, base: starknet::StorageBaseAddress, value: Position
        ) -> starknet::SyscallResult::<()> {
            starknet::storage_write_syscall(
                address_domain, starknet::storage_address_from_base_and_offset(base, 0_u8), value.x
            )?;
            starknet::storage_write_syscall(
                address_domain, starknet::storage_address_from_base_and_offset(base, 1_u8), value.y
            )
        }
    }


    struct Storage {
        state: LegacyMap::<(felt252, felt252, felt252, felt252), Position>, 
    }

    // Initialize Position.
    #[external]
    fn initialize() {}

    // Set the state of an entity.
    #[external]
    fn set(entity_id: (felt252, felt252, felt252, felt252), value: Position) {
        state::write(entity_id, value);
    }

    // Get the state of an entity.
    #[view]
    fn get(entity_id: (felt252, felt252, felt252, felt252)) -> Position {
        return state::read(entity_id);
    }
}

#[derive(Copy, Drop)]
struct Player {
    name: felt252, 
}

#[abi]
trait IPlayer {
    fn set(entity_id: (felt252, felt252, felt252, felt252), value: Player);
    fn get(entity_id: (felt252, felt252, felt252, felt252)) -> Player;
}

#[contract]
mod PlayerComponent {
    use option::OptionTrait;
    use starknet::SyscallResult;
    use traits::Into;
    use traits::TryInto;
    use super::Player;

    impl PlayerSerde of serde::Serde::<Player> {
        fn serialize(ref serialized: Array::<felt252>, input: Player) {
            serde::Serde::<felt252>::serialize(ref serialized, input.name);
        }
        fn deserialize(ref serialized: Span::<felt252>) -> Option::<Player> {
            Option::Some(Player { name: serde::Serde::<felt252>::deserialize(ref serialized)?,  })
        }
    }

    impl StorageAccessPlayer of starknet::StorageAccess::<Player> {
        fn read(
            address_domain: u32, base: starknet::StorageBaseAddress
        ) -> starknet::SyscallResult::<Player> {
            Result::Ok(
                Player {
                    name: starknet::storage_read_syscall(
                        address_domain, starknet::storage_address_from_base_and_offset(base, 0_u8)
                    )?,
                }
            )
        }
        fn write(
            address_domain: u32, base: starknet::StorageBaseAddress, value: Player
        ) -> starknet::SyscallResult::<()> {
            starknet::storage_write_syscall(
                address_domain,
                starknet::storage_address_from_base_and_offset(base, 0_u8),
                value.name
            )
        }
    }


    struct Storage {
        state: LegacyMap::<(felt252, felt252, felt252, felt252), Player>, 
    }

    // Initialize Player.
    #[external]
    fn initialize() {}

    // Set the state of an entity.
    #[external]
    fn set(entity_id: (felt252, felt252, felt252, felt252), value: Player) {
        state::write(entity_id, value);
    }

    // Get the state of an entity.
    #[view]
    fn get(entity_id: (felt252, felt252, felt252, felt252)) -> Player {
        return state::read(entity_id);
    }
}

#[contract]
mod SpawnSystem {
    use dojo::world;
    use dojo::world::IWorldDispatcher;
    use dojo::world::IWorldDispatcherTrait;


    use traits::Into;
    use starknet::contract_address::ContractAddressIntoFelt252;

    use dojo::commands::Spawn;
    use super::Position;
    use super::IPositionDispatcher;
    use super::IPositionDispatcherTrait;
    use super::Player;
    use super::IPlayerDispatcher;
    use super::IPlayerDispatcherTrait;

    #[external]
    fn execute(name: felt252) {
        let world_address = starknet::contract_address_const::<0x0>();
        let entity_id = IWorldDispatcher {
            contract_address: world_address
        }.next_entity_id(((0, 0, 420)));
        IPlayerDispatcher {
            contract_address: starknet::contract_address_const::<0x45ae1fe01b0daa1b91e29c079ddcc83a8aae082c898bc23dfb766ab12b0403a>()
        }.set(entity_id, Player { name: name });
        IPositionDispatcher {
            contract_address: starknet::contract_address_const::<0x3718a0ff90de39b4acf137c1a27b0427c2c3f60c23c4f7c20b85c578d2c5897>()
        }.set(entity_id, Position { x: 0, y: 0 });
        IPlayerDispatcher {
            contract_address: starknet::contract_address_const::<0x45ae1fe01b0daa1b91e29c079ddcc83a8aae082c898bc23dfb766ab12b0403a>()
        }.set((0, 0, 0, 420), Player { name: name });
        IPositionDispatcher {
            contract_address: starknet::contract_address_const::<0x3718a0ff90de39b4acf137c1a27b0427c2c3f60c23c4f7c20b85c578d2c5897>()
        }.set((0, 0, 0, 420), Position { x: 0, y: 0 });

        let player_id = starknet::get_caller_address();
        IPlayerDispatcher {
            contract_address: starknet::contract_address_const::<0x45ae1fe01b0daa1b91e29c079ddcc83a8aae082c898bc23dfb766ab12b0403a>()
        }.set((0, 0, 420, player_id.into()), Player { name: name });
        IPositionDispatcher {
            contract_address: starknet::contract_address_const::<0x3718a0ff90de39b4acf137c1a27b0427c2c3f60c23c4f7c20b85c578d2c5897>()
        }.set((0, 0, 420, player_id.into()), Position { x: 0, y: 0 });
        return ();
    }
}

#[contract]
mod MoveSystem {
    use dojo::world;
    use dojo::world::IWorldDispatcher;
    use dojo::world::IWorldDispatcherTrait;


    use array::ArrayTrait;
    use dojo::query::Query;
    use super::Position;
    use super::IPositionDispatcher;
    use super::IPositionDispatcherTrait;
    use super::Player;
    use super::IPlayerDispatcher;
    use super::IPlayerDispatcherTrait;

    fn move_inner(positions: @Array<usize>, index: u32, count: u32) {
        return move_inner(positions, index + 1_u32, count - 1_u32);
    }

    #[external]
    fn execute(player_id: felt252) {
        let world_address = starknet::contract_address_const::<0x0>();
        let positions_query = ArrayTrait::<usize>::new();
        let positions_query_position_ids = IWorldDispatcher {
            contract_address: world_address
        }.get_entities(
            starknet::contract_address_const::<0x3718a0ff90de39b4acf137c1a27b0427c2c3f60c23c4f7c20b85c578d2c5897>(),
            (0, 0, 0)
        );

        let positions_query_player_ids = IWorldDispatcher {
            contract_address: world_address
        }.get_entities(
            starknet::contract_address_const::<0x45ae1fe01b0daa1b91e29c079ddcc83a8aae082c898bc23dfb766ab12b0403a>(),
            (0, 0, 0)
        );
        let players_query = ArrayTrait::<usize>::new();
        let players_query_player_ids = IWorldDispatcher {
            contract_address: world_address
        }.get_entities(
            starknet::contract_address_const::<0x45ae1fe01b0daa1b91e29c079ddcc83a8aae082c898bc23dfb766ab12b0403a>(),
            (0, 0, 0)
        );

        let player_player = IPlayerDispatcher {
            contract_address: starknet::contract_address_const::<0x45ae1fe01b0daa1b91e29c079ddcc83a8aae082c898bc23dfb766ab12b0403a>()
        }.get((0, 0, 0, player_id));
        let player = player_player;

        let mut bar = 123;
        let mut foo = ArrayTrait::<felt252>::new();
        foo.append(bar);

        move_inner(@positions_query, 0_u32, positions_query.len());

        if bar == 123 {
            return ();
        } else if bar == 0 {
            return ();
        } else {
            return ();
        }
    }
}

//! > expected_diagnostics
error: Plugin diagnostic: Invalid arguements. Expected "(entity_path, components)"
 --> lib.cairo:45:41
        let invalid_args = Spawn::bundle((
                                        ^^

error: Plugin diagnostic: Entity path too long
 --> lib.cairo:50:42
        let invalid_path = Spawn::bundle((0, 0, 0, 0, 0), (
                                         ^*************^
