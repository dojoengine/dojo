[package]
name = "dojo_erc"
version = "0.1.0"
cairo-version = "1.1.0"
description = "Implementations of ERC standards for the Dojo framework"

[dependencies]
dojo_core = { path = "../dojo-core" }

[[target.dojo]]
