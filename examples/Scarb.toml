[package]
name = "dojo_examples"
version = "0.1.0"

[dependencies]
dojo = { path = "../lib" }

[[target.dojo]]

[tool.dojo]
world_address = "0xdead"
initializer_class_hash = "0xbeef"

[tool.dojo.testnet]
rpc = "https://rpc.testnet.starkware.co"
