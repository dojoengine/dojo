// Simulates outcome of two Armies

#[system]
mod CombatSystem {
    use array::ArrayTrait;
    use traits::Into;

    fn execute(
        attacking_realm_id: felt252,
        defending_realm_id: felt252,
        attacking_army_id: felt252,
        defending_army_id: felt252
    ) {
        // checks ownership

        // check same location
        
        // resolve battle

        // pillage resources accordingly

    }
}
